version: '3.8'

services:
  tts-bot:
    build: .
    container_name: tts-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
    volumes:
      - ./logs:/app/logs
    # webhook mode
    # ports:
    #   - "8443:8443"
